name = "mate-reset-ai-backend"
main = "server.ts"
compatibility_date = "2024-03-01"
compatibility_flags = ["nodejs_compat"]

# Environment variables
# GEMINI_API_KEY is secret, so it shouldn't be added here. Set it via the dashboard or:
# npx wrangler secret put GEMINI_API_KEY
